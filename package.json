{
  "dependencies": {
    "bootstrap": "^4.5.0"
  },
  "devDependencies": {
    "autoprefix": "^1.0.1",
    "autoprefixer": "^9.8.0",
    "cssnano": "^4.1.10",
    "postcss-cli": "^7.1.1",
    "postcss-import": "^12.0.1",
    "sass": "^1.26.8",
    "serve": "^11.3.2"
  },
  "private": true,
  "scripts": {
    "build": "sass --no-source-map styles.scss styles.css && postcss styles.css > styles.min.css",
    "start": "serve"
  }
}
